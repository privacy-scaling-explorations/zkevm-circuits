version: '3'
services:
    bench:
      build:
        context: .
        dockerfile: BENCH
        args:
          BENCH_NAME: evm_prover
      container_name: bench
      tty: true
      stdin_open:  true
      volumes:
        - .:/app
        - /app/target
