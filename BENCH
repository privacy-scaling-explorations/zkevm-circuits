FROM shinsaku0523/zkevm:latest

ARG BENCH_NAME

ENV BENCH_NAME $BENCH_NAME

COPY . .

RUN RUSTFLAGS='-C target-cpu=native' cargo test --no-run --profile bench

CMD RUSTFLAGS='-C target-cpu=native' cargo test --profile bench $BENCH_NAME -- --nocapture --ignored
